# BridgeLabz Training - Assignment 9
## Responsive CSS

This folder contains separate HTML and CSS files for Questions 1-10.

Rules followed:
- Separate HTML and CSS file for every question
- No JavaScript
- No external CSS framework
- Semantic HTML5 tags
- Flexbox/Grid for layout
- Responsive media queries
- Hover transitions and animations where required

Note: Q1, Q5, Q7 and Q8 use online image URLs, so the images require an internet connection when opened in a browser.
